apiVersion: v1
kind: Service
metadata:
  name: tlc
  labels:
    app: tlc
spec:
  type: NodePort # Ou LoadBalancer selon l'environnement
  ports:
  - name: http
    port: 80
    targetPort: 80
  - name: https
    port: 443
    targetPort: 443
  selector:
    app: tlc
